<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Not requestable through the normal runtime-permission dialog. Declaring it here is what
      lets `adb shell pm grant` succeed once, after which this app can recover wireless debugging
      when Android disables it during a network transition.
    -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ gates NSD service resolution behind this; declared neverForLocation since
         we only use it to find our own adbd's advertised port, not to infer physical position. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <!-- Android 13+ requires this to show the wakeup-request notification at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- RelayShareService: keeps "share my phone" bridging alive once the app is backgrounded.
         dataSync fits best of Android 14's enumerated foreground-service types: this is
         proxying this device's own data (adb traffic) over the network in the background, not
         playing media, tracking location, or anything else with its own dedicated type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".BridgeApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".messaging.BridgeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".relay.RelayShareService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
